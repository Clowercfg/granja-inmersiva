# ui/

Imágenes de interfaz (opcional):

- `logo.png`    — Logo del juego (HUD / pantalla de carga).
- `icon-*.png`  — Iconos de paneles.
- `panel-bg.png`— Fondos de tarjeta.

Estas imágenes no se cargan todavía desde código; se consumen desde
`src/ui/` cuando se necesiten.
