# environment/

Props decorativos del mundo. Archivos esperados (nombres exactos):

- `fence.glb`      — Valla decorativa (placeholder: se construye proceduralmente).
- `lamp.glb`       — Farola.
- `bench.glb`      — Banco.
- `haybale.glb`    — Paca de paja.
- `trough.glb`     — Abrevadero.
- `well.glb`       — Pozo.

Coloca aquí cualquier prop nuevo y registra su clave en
`src/core/assets/assetConfig.ts`.
