# trees/

Modelos de árboles. Archivos esperados (nombres exactos):

- `tree.glb`   — Árbol genérico (se instancia por todo el mapa con variación
  de escala, rotación y tinte de color por instancia).

Un solo modelo basta; la vegetación lo reutiliza con `InstancedMesh`.
